import { MAX_PASSWORD_LENGTH, MIN_PASSWORD_LENGTH } from "./index";

export const MIN_MAX_PASS = `Password must be ${MIN_PASSWORD_LENGTH}-${MAX_PASSWORD_LENGTH} characters.`

export const ENTER_VALID_EMAIL = "Please enter a valid email.";